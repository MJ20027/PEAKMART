// const Redis = require("ioredis");

// const getRedisUrl = () => {
//   if (process.env.REDIS_URL) {
//     return process.env.REDIS_URL;
//   }
//   throw new Error("url not defined");
// }

// module.exports.redis = new Redis({connectTimeout:1000000},getRedisUrl());